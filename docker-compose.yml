services:
  app:
    build: .
    container_name: ai-chatbot
    network_mode: host
    env_file:
      - .env
    volumes:
      - app_data:/app/data
    restart: unless-stopped

volumes:
  app_data:
